{
  "name": "barcode-inventory-system",
  "version": "1.0.0",
  "description": "Barcode inventory management system",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "init-db": "cd backend && npm run init-db",
    "kill-port": "node scripts/kill-port.js",
    "clean": "node scripts/clean.js",
    "dev": "npm run kill-port && cd backend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "npm run kill-port && cd backend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["barcode", "inventory", "management"],
  "author": "",
  "license": "MIT"
}
